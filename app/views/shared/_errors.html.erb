<% if @errors.present? && @chapters_with_errors %>
  <div class="alert alert-danger">
    <button class="close" data-dismiss="alert">×</button>
    <% @errors.each do |error| %>
      <%= error %><br/>
    <% end %>
    <%= t('messages.chapters_with_errors') %><%= @chapters_with_errors %>
  </div>
<% end %>